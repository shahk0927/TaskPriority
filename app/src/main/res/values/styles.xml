<resources>
    <!-- Base application theme. -->
    <!-- Light theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="backgroundColor">@color/colorWhite</item>
        <item name="textColor">@color/black</item>
    </style>

    <!-- Dark theme. -->
    <style name="DarkAppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">#212121</item>
        <item name="colorPrimaryDark">@color/black</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="backgroundColor">#303030</item>
        <item name="textColor">@color/colorWhite</item>
    </style>


    <style name="NumberPicker" parent="Theme.AppCompat.Light">
        <!-- Customize your theme here. -->

        <item name="colorAccent">@color/colorAccent</item>
        <item name="colorControlNormal">#212121</item>
        <item name="android:textColorPrimary">@color/gradient_end_btn</item>
        <item name="android:background">@android:color/transparent</item>
        <item name="android:textSize">18sp</item>
    </style>

    <style name="MenuPadding" parent="TextAppearance.AppCompat.Widget.ActionBar.Menu">
        <item name="android:layout_marginRight">100dp</item>

    </style>
</resources>